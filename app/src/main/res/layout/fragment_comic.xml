<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="edu.ucsb.cs.cs190i.giovannidominguez.masterdetaildemo.Comic">

    <!-- TODO: Update blank fragment layout -->

    <edu.ucsb.cs.cs190i.giovannidominguez.masterdetaildemo.TouchImage
        android:id="@+id/thecomic"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:scaleType="fitStart"
        android:adjustViewBounds="true"
        android:layout_above="@+id/editText"

        />

    <EditText
        android:id="@+id/editText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/getCom"
        android:layout_alignParentStart="true"
        android:hint="Enter a number between 1 and 1826"
        android:imeOptions="actionDone"
        android:inputType="numberDecimal" />

    <Button
        android:id="@+id/getCom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:text="Get Comic"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:imeOptions="actionDone"
 />

</RelativeLayout>
